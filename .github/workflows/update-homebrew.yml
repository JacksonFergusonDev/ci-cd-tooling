name: Update Homebrew Tap

on:
  workflow_call:
    inputs:
      tag:
        description: 'The release tag (e.g., v1.0.0)'
        required: true
        type: string
      package_name:
        description: 'The PyPI package name (optional, inferred from caller pyproject.toml)'
        required: false
        type: string
        default: ''
      formula_path:
        description: 'Relative path to the formula in the tap (optional, defaults to Formula/<package_name>.rb)'
        required: false
        type: string
        default: ''
    secrets:
      TAP_GITHUB_TOKEN:
        required: true

jobs:
  update-homebrew:
    name: Sync Formula & Python Resources
    runs-on: ubuntu-latest
    env:
      HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
    steps:
      - name: Check out Caller Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          path: caller-repo

      - name: Check out Homebrew Tap
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: JacksonFergusonDev/homebrew-tap
          token: ${{ secrets.TAP_GITHUB_TOKEN }}
          path: homebrew-tap

      - name: Check out Tooling Repo (for the Python script)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: JacksonFergusonDev/ci-cd-release-infrastructure
          path: tooling-repo

      - name: Install uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          python-version: "3.14"

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@304fc86d2c26bcd269547dbcfcfb4c853e710779 # main

      - name: Trust the tap
        run: brew trust JacksonFergusonDev/tap

      - name: Tap the repository
        run: brew tap JacksonFergusonDev/tap

      - name: Update Formula
        id: update
        run: |
          ARGS=(--version "${{ inputs.tag }}" --caller-dir "caller-repo" --tap-dir "homebrew-tap")
          if [ -n "${{ inputs.package_name }}" ]; then
            ARGS+=(--package "${{ inputs.package_name }}")
          fi
          if [ -n "${{ inputs.formula_path }}" ]; then
            ARGS+=(--formula-path "${{ inputs.formula_path }}")
          fi
          uv run tooling-repo/scripts/update_homebrew.py "${ARGS[@]}"

      - name: Commit and Push
        working-directory: homebrew-tap
        run: |
          FORMULA_PATH="${{ steps.update.outputs.formula_path }}"
          FORMULA_NAME="${{ steps.update.outputs.formula_name }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$FORMULA_PATH"

          if git diff --cached --quiet; then
            echo "No formula changes to commit."
            exit 0
          fi

          cp "$FORMULA_PATH" "$(brew --repository jacksonfergusondev/tap)/Formula/"

          brew audit --strict --online "jacksonfergusondev/tap/$FORMULA_NAME"
          git commit -m "chore: bump $FORMULA_NAME to ${{ inputs.tag }} and sync python resources"
          git push
